# BrainDrive Frontend Configuration Template
# This file is used to generate the frontend .env file during installation

# API Configuration
VITE_API_URL=http://{BACKEND_HOST}:{BACKEND_PORT}
VITE_API_TIMEOUT=60000

# Force production mode to avoid hardcoded development IP
NODE_ENV=production
VITE_MODE=production

# Application Settings
VITE_APP_NAME="BrainDrive"
VITE_APP_VERSION="1.0.0
VITE_APP_DESCRIPTION="Advanced AI platform with React frontend and FastAPI backend"

# Development Settings
VITE_DEV_SERVER_PORT={FRONTEND_PORT}
VITE_DEV_SERVER_HOST={FRONTEND_HOST}
VITE_DEV_SERVER_OPEN=false

# Build Settings
VITE_BUILD_SOURCEMAP=false
VITE_BUILD_MINIFY=true

# Feature Flags
VITE_ENABLE_PWA={ENABLE_PWA}
VITE_ENABLE_ANALYTICS={ENABLE_ANALYTICS}
VITE_ENABLE_DEBUG={DEBUG_MODE}

# UI Settings
VITE_THEME_DEFAULT="{DEFAULT_THEME}"
VITE_THEME_ALLOW_TOGGLE=true

# Security
VITE_ENABLE_CSP=true
VITE_SECURE_COOKIES=true

# Performance
VITE_ENABLE_LAZY_LOADING=true
VITE_CHUNK_SIZE_WARNING_LIMIT=1000

# Logging
VITE_LOG_LEVEL="warn"
VITE_ENABLE_CONSOLE_LOGS=false
