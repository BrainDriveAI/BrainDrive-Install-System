# Core dependencies
dulwich==0.22.6
Pillow==11.0.0
psutil==6.1.0
Requests==2.32.3

# Platform-specific dependencies
pywin32==308; sys_platform == "win32"

# Git operations for BrainDrive repository management
GitPython==3.1.40

# Enhanced process management
subprocess32==3.5.4; python_version < "3.0"

# Cross-platform path and system utilities
pathlib2==2.3.7; python_version < "3.4"

# PyInstaller for executable generation (development dependency)
pyinstaller>=6.0.0
pyinstaller-hooks-contrib>=2023.10

# Additional utilities for BrainDrive installer
packaging>=21.0
setuptools>=60.0
wheel>=0.37.0

# Network and HTTP utilities
urllib3>=1.26.0
certifi>=2021.10.8

# JSON and configuration handling
jsonschema>=4.0.0

# Logging and debugging
colorama>=0.4.4
tqdm>=4.64.0

# PyInstaller build dependencies
altgraph>=0.17.3
pefile>=2023.2.7; sys_platform == "win32"
macholib>=1.16; sys_platform == "darwin"

# Build optimization tools
# Note: UPX is not available as a Python package on Windows
# For Windows, UPX should be installed separately if compression is needed
# upx-ucl>=4.0.0; sys_platform == "linux"  # UPX compression (Linux only via pip)

# Additional hidden imports support
importlib-metadata>=4.0.0
typing-extensions>=4.0.0

# GUI framework dependencies (ensure tkinter support)
tk>=0.1.0; sys_platform == "linux"

# Version information and metadata
pefile>=2023.2.7; sys_platform == "win32"
